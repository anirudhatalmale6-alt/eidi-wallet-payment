// EUDI Wallet Waterloo - Powered by NetSwitch v2.3.0
// AI2ORBIT Co. 2026. All rights reserved.

plugins {
    id 'com.android.application'
}

android {
    namespace 'com.ai2orbit.eidiwalletpayment'
    compileSdk 35

    defaultConfig {
        applicationId "com.ai2orbit.eidiwalletpayment"
        minSdk 24
        targetSdk 35
        versionCode 1
        versionName "1.0.0"
    }

    signingConfigs {
        release {
            storeFile file('keystore.jks')
            storePassword 'benchmark2026'
            keyAlias 'benchmark'
            keyPassword 'benchmark2026'
        }
    }

    buildTypes {
        release {
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.release
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.6.1'
    implementation 'com.google.android.material:material:1.9.0'
}
